/**
 * Analytics view (route `/analytics`). Placeholder for now — the detailed
 * analytics charts land later. Shares the `analytics` permission/resource with
 * the dashboard (ADR-0002).
 */
export function AnalyticsView() {
  return (
    <div className="flex h-full items-center justify-center text-2xl font-semibold text-muted-foreground">
      Analytics
    </div>
  );
}
