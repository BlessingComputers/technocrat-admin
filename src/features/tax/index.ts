// Public API of the tax feature.
// Export ONLY what routes or other layers need. Everything else stays internal.

// The Tax tab of `/pricing`, composed by the app-layer pricing workspace.
export { TaxWorkspaceView } from "./components/tax-workspace-view";
