// Public API of the whatsapp feature (the WhatsApp Business inbox).
// Export ONLY what routes or other layers need. Everything else stays internal.

export { WhatsAppWorkspaceView } from "./components/whatsapp-workspace-view";
